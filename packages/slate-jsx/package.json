{
  "name": "@mattiaz9/slate-jsx",
  "version": "0.1.2",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "module": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts-resolve --minify --clean --sourcemap --dts",
    "dev": "tsup src/index.ts --format esm --dts-resolve --clean --watch --sourcemap --dts",
    "lint": "eslint --ext .ts,.tsx src"
  },
  "dependencies": {
    "slate": "^0.100.0"
  },
  "devDependencies": {
    "@types/react": "18.2.31",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "tsup": "7.2.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
